#cloud-config
package_update: true
packages:
  - git
  - python3
  - python3-pip
runcmd:
  - pip3 install ansible
  - ansible-pull -U https://github.com/kashiwai/playbook.git ansible/playbook.yml -i "localhost," -c local
